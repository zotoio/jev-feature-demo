/** TypeSafe API base URL (override with TYPESAFE_BASE_URL). */
export const TYPESAFE_API_BASE = "https://api.typesafe.ai";

/** Default alias — resolves to the current stable release per docs. */
export const JEV_LATEST = "jev-latest";

/** Pinned stable model ID per https://docs.typesafe.ai/models */
export const JEV_PINNED = "jev-1.13.0";

export const SYSTEMONE_PATH = "/v1/systemone";
export const MODELS_PATH = "/v1/models";
