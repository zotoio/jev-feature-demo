# Optional — offline tests use fixtures; set this for pnpm test:live and the UI dev server.
# Read by Node/Vite on the dev server only — never exposed in the browser bundle.
# Copy to .env or .env.local (both gitignored) for local runs.
TYPESAFE_API_KEY=
